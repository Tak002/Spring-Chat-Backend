services:
  postgres:
    ports: ["5432:5432"]
    restart: unless-stopped

  redis:
    ports: ["6379:6379"]
    restart: unless-stopped

  app-chat-ws:
    image: ${DOCKER_IMAGE:-tak002/spring-chat-server}:${IMAGE_TAG:-latest}
    ports: ["8080:8080"]
    pull_policy: always
    restart: unless-stopped
